# Tennis Kata

Solution was developed using Test Driven Development.

The implementation and tests are kept together in a single Python file.

## Requirements:

- Python3
- pytest

## Setup

Create and activate a virtual environment:

python3 -m venv .venv
source .venv/bin/activate

Install the required dependency:

pip install -r requirements.txt

Running the tests:

pytest tennis_kata.py -v